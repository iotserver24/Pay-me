# Payment Microservice Frontend

Next.js + Tailwind CSS frontend for payment processing.

## Setup

1. Copy `example.env` to `.env.local` (or .env).
2. `npm install`
3. `npm run dev`

## Pages

- `/pay/[id]`: Payment page
- `/admin/login`: Admin login
- `/admin`: Admin dashboard
